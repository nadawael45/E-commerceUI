import 'package:flutter/material.dart';
class FavView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text('favorite'),),);
  }
}
